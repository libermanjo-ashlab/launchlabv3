{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "sleep 3 && (cd server && npx prisma migrate deploy) && node server/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
