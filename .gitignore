node_modules/
.env
*.db
*.db-journal
dist/
.DS_Store
*.log
.vscode/settings.json

# NOTE: server/prisma/migrations/ is intentionally NOT ignored.
# Prisma needs committed migration files for `migrate deploy` to work on Railway.
