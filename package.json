{
  "name": "earnedlab",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "postinstall": "npm install --prefix server --legacy-peer-deps && (cd server && npx prisma generate) && npm install --prefix client --legacy-peer-deps && npm run build --prefix client",
    "start": "(cd server && npx prisma migrate deploy) && node server/index.js",
    "dev:server": "cd server && node index.js",
    "dev:client": "cd client && npx vite"
  }
}
